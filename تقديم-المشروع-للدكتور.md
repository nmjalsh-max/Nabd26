# 🎓 مشروع التخرج: مساحة نبض (Nabd Space)
## نظام إدارة الصحة النفسية للموظفين في بيئة العمل
### Bachelor of Information Technology — Systems Development and Administration

---

## 📋 جدول المحتويات
1. [فكرة المشروع](#-فكرة-المشروع)
2. [المشكلة والحل](#-المشكلة-والحل)
3. [المميزات الرئيسية](#-المميزات-الرئيسية)
4. [الفئة المستهدفة](#-الفئة-المستهدفة)
5. [التقنيات المستخدمة (Tech Stack)](#-التقنيات-المستخدمة-tech-stack)
6. [هيكلة المشروع (Architecture)](#-هيكلة-المشروع-architecture)
7. [قاعدة البيانات (Database Schema)](#-قاعدة-البيانات-database-schema)
8. [واجهات المستخدم (Pages & Routes)](#-واجهات-المستخدم-pages--routes)
9. [المستخدمون والأدوار (Roles)](#-المستخدمون-والأدوار-roles)
10. [سير العمل (User Flow)](#-سير-العمل-user-flow)
11. [صور توضيحية للواجهات](#-صور-توضيحية-للنظام)
12. [كيفية تشغيل المشروع](#-كيفية-تشغيل-المشروع)
13. [التوسعات المستقبلية (Future Work)](#-التوسعات-المستقبلية-future-work)
14. [فريق العمل](#-فريق-العمل)
15. [الملحق: هيكل المجلدات الكامل](#-الملحق-هيكل-المجلدات)

---

## 💡 فكرة المشروع

**نبض** هو نظام ويب متكامل (Web Application) لإدارة الصحة النفسية للموظفين داخل الشركات والمؤسسات. يهدف إلى **رعاية معنويات الموظفين** من خلال:

- 📊 قياس يومي للمشاعر والمزاج (Daily Mood/Pulse Check-in)
- 🤝 متابعة داعمة وغير وصمية للحالات الحرجة
- ⭐ نظام نقاط ومكافآت لتحفيز المشاركة
- 🧘 حجز جلسات استرخاء (يوغا، تنفس جماعي)
- 📈 تحليلات وتقارير دقيقة للإدارة

> **الشعار**: "رعاية معنويات الموظفين — بهدوء واهتمام"

---

## 🔍 المشكلة والحل

### المشكلة:
- %70 من الموظفين يعانون من الإرهاق النفسي في العمل
- ضعف التواصل بين الإدارة والموظفين حول الصحة النفسية
- عدم وجود أدوات رقمية لمتابعة المعنويات بشكل منتظم
- الوصم الاجتماعي (Stigma) المرتبط بطلب المساعدة النفسية

### الحل — مساحة نبض:
- نظام يومي بسيط للقياس دون ضغط
- لغة داعمة ومشجعة بدلاً من لغة إنذارية
- مكافآت تحفّز على الاستمرار
- خصوصية تامة للموظف (يرى فقط بياناته)
- لوحة متكاملة للإدارة لمتابعة الفريق

---

## ✨ المميزات الرئيسية

### 👤 لوحة الموظف (Employee Dashboard)
| الميزة | الوصف |
|--------|-------|
| **المقياس اليومي** | 5 أسئلة بسيطة عن الطاقة، التوتر، التركيز، الدعم، المزاج |
| **نظام النقاط** | يحصل الموظف على 25 نقطة يومياً عند إكمال المقياس |
| **المكافآت** | شارات ومكافآت عند الوصول لعتبات محددة (300, 400, 500 نقطة) |
| **حجز الجلسات** | حجز جلسات اليوغا والتأمل حسب السعة المتاحة |
| **الإشعارات** | إشعارات فورية عند إضافة النقاط أو فتح مكافأة جديدة |

### 👑 لوحة الأدمن (Admin Dashboard)
| الميزة | الوصف |
|--------|-------|
| **نسبة المشاركة اليومية** | معرفة عدد الموظفين الذين أكملوا المقياس |
| **إجمالي النقاط الموزعة** | إحصائيات تفاعل الموظفين |
| **حالات المتابعة** | حالات حرجة بحاجة تدخل (Critical Alerts) |
| **التقارير** | تقارير أسبوعية/شهرية/سنوية مع تصدير PDF, CSV |
| **التحليلات** | رسومات بيانية (Line, Bar, Pie Charts) لمقارنة الأقسام |
| **رفع الملفات** | رفع بيانات الموظفين (CSV) |

---

## 🎯 الفئة المستهدفة

- **شركات التقنية والمؤسسات** التي لديها أكثر من 10 موظفين
- **إدارة الموارد البشرية** (HR Admin) المسؤولة عن رفاهية الموظفين
- **الموظفون** أنفسهم للحصول على دعم يومي

---

## 🛠 التقنيات المستخدمة (Tech Stack)

### Frontend
| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| React | 18.3.1 | بناء واجهات المستخدم (UI) |
| TypeScript | 5.5.2 | أمان الأنواع (Type Safety) |
| Vite | 5.3.1 | بناء وتشغيل التطبيق (Build Tool) |
| TailwindCSS | 3.4.4 | تصميم سريع (Utility-first CSS) |
| React Router | 7.18.1 | التنقل بين الصفحات (Routing) |
| Recharts | 2.12.7 | الرسوم البيانية (Charts) |
| Lucide React | 0.400.0 | الأيقونات (Icons) |

### Backend / Database
| التقنية | الاستخدام |
|---------|-----------|
| Supabase | قاعدة بيانات PostgreSQL + Auth + Storage |
| Supabase Auth | تسجيل الدخول وإدارة الجلسات |
| Row Level Security (RLS) | صلاحيات على مستوى الصفوف |

### State Management & Data
| النوع | الوصف |
|-------|--------|
| **Mock Data** | بيانات وهمية مدمجة (افتراضي) — لا يحتاج Backend |
| **Supabase** | قاعدة بيانات حقيقية عند توفر المتغيرات البيئية |
| Local Storage | تخزين الجلسات محلياً للمعاينة |

### أخرى
| الميزة | الوصف |
|--------|-------|
| **RTL/LTR** | دعم اللغة العربية (يمين-يسار) والإنجليزية |
| **Dark Theme** | تصميم داكن بألوان الخزامي والوردي |
| **Responsive** | متجاوب مع جميع أحجام الشاشات |

---

## 🏗 هيكلة المشروع (Architecture)

```
nabd-hr-admin/
├── src/
│   ├── main.tsx              # نقطة البداية — Boot + Lang
│   ├── App.tsx               # توجيه المسارات (Router)
│   ├── index.css             # أنماط TailwindCSS
│   │
│   ├── theme/
│   │   └── tokens.ts         # نظام الألوان الموحّد (Design Tokens)
│   │
│   ├── auth/
│   │   ├── BootContext.tsx    # سياق التحقق من الجلسة (Auth Context)
│   │   └── ProtectedRoute.tsx # حماية المسارات حسب الدور
│   │
│   ├── components/
│   │   ├── ui/               # مكونات UI عامة (Button, Card, Input...)
│   │   ├── AppShell.tsx      # الهيكل الأساسي بعد تسجيل الدخول
│   │   ├── AdminUI.tsx       # مكونات خاصة بلوحة الأدمن
│   │   ├── LangToggle.tsx    # زر تبديل اللغة
│   │   ├── HeartLoader.tsx   # منتقي التحميل بشكل قلب نابض
│   │   └── DataState.tsx     # إدارة حالات التحميل/البيانات/الفارغ
│   │
│   ├── i18n/
│   │   ├── i18n.ts           # قاموس الترجمة (عربي / إنجليزي)
│   │   └── LangContext.tsx    # سياق اللغة
│   │
│   ├── lib/
│   │   ├── supabaseClient.ts # اتصال Supabase
│   │   └── dashboardData.ts  # منطق الأعمال (Business Logic)
│   │
│   ├── mock-data/            # بيانات وهمية لكل كيان
│   │   ├── auth.ts, admin.ts, employee.ts, mood.ts
│   │   ├── points.ts, sessions.ts, reports.ts
│   │   ├── analytics.ts, uploads.ts, notifications.ts
│   │   └── landing.ts
│   │
│   ├── pages/                # صفحات التطبيق
│   │   ├── Landing.tsx       # الصفحة الرئيسية
│   │   ├── Login.tsx         # تسجيل الدخول
│   │   ├── Signup.tsx        # إنشاء حساب
│   │   ├── EmployeeDashboard.tsx
│   │   ├── AdminDashboard.tsx
│   │   ├── MoodQuestions.tsx
│   │   ├── PointsRewards.tsx
│   │   ├── SessionsCalendar.tsx
│   │   ├── Reports.tsx
│   │   ├── AnalyticsMonitoring.tsx
│   │   ├── AnalyticsMonitoring2.tsx
│   │   ├── UploadFiles.tsx
│   │   ├── NotificationSystem.tsx
│   │   └── HeartLoaderPage.tsx
│   │
│   └── styles/
│       └── fonts.css         # خطوط عربية/إنجليزية
│
├── supabase/
│   └── migrations/
│       └── 001_init_hr_schema.sql # هيكل قاعدة البيانات
│
└── ملفات الإعدادات
    ├── package.json          # التبعيات
    ├── tsconfig.json         # إعدادات TypeScript
    ├── vite.config.ts        # إعدادات Vite
    ├── tailwind.config.js    # إعدادات Tailwind
    ├── postcss.config.js     # إعدادات PostCSS
    └── eslint.config.js      # إعدادات التحليل
```

### 🧩 تصميم العمارة (Architecture Pattern)

```
┌──────────────────────────────────────────────┐
│                 Browser                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │   Pages  │  │  Components│  │   Auth   │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │             │             │          │
│  ┌────▼─────────────▼─────────────▼─────┐    │
│  │          Business Logic              │    │
│  │       (dashboardData.ts)             │    │
│  └────────────────┬─────────────────────┘    │
│                   │                          │
│  ┌────────────────▼─────────────────────┐    │
│  │        Data Layer                    │    │
│  │  ┌───────────┐  ┌────────────────┐   │    │
│  │  │ Mock Data │  │  Supabase DB   │   │    │
│  │  │ (Local)   │  │  (Optional)    │   │    │
│  │  └───────────┘  └────────────────┘   │    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

**ملاحظة هامة**: النظام يعمل بشكل كامل **بدون إعداد Backend** — يستخدم Mock Data مدمجة افتراضياً. وعند توفير متغيرات Supabase، يتحول تلقائياً لاستخدام قاعدة البيانات الحقيقية.

---

## 🗄 قاعدة البيانات (Database Schema)

### جدول المستخدمين (users)
```sql
create table users (
  id uuid primary key references auth.users(id),
  full_name text not null,
  email text unique not null,
  role text not null check (role in ('admin','employee')),
  department text,
  employee_number text unique
);
```

### جدول الأسئلة (pulse_questions)
```sql
create table pulse_questions (
  id bigserial primary key,
  question_key text unique not null,
  question_text text not null,
  is_active boolean default true
);
```

### جدول الإجابات (pulse_responses)
```sql
create table pulse_responses (
  id bigserial primary key,
  user_id uuid references users(id),
  pulse_question_id bigint references pulse_questions(id),
  response_value integer,
  created_at timestamptz default now()
);
```

### جدول النقاط (points_ledger)
```sql
create table points_ledger (
  id bigserial primary key,
  user_id uuid references users(id),
  points_delta integer not null,
  reason text,
  created_at timestamptz default now()
);
```

### جدول المكافآت (rewards + user_rewards)
```sql
create table rewards (
  id bigserial primary key,
  title text not null,
  points_cost integer not null,
  is_active boolean default true
);

create table user_rewards (
  id bigserial primary key,
  user_id uuid references users(id),
  reward_id bigint references rewards(id),
  status text default 'pending',
  unique(user_id, reward_id)
);
```

### جدول الجلسات (yoga_sessions + session_attendance)
```sql
create table yoga_sessions (
  id bigserial primary key,
  title text not null,
  mode text, coach text,
  starts_at timestamptz not null,
  capacity integer default 1,
  is_active boolean default true
);

create table session_attendance (
  id bigserial primary key,
  user_id uuid references users(id),
  session_id bigint references yoga_sessions(id),
  status text default 'registered',
  unique(user_id, session_id)
);
```

### جدول الإشعارات (notifications + critical_alerts)
```sql
create table notifications (
  id bigserial primary key,
  user_id uuid references users(id),
  type text not null,
  title text not null,
  body text,
  is_read boolean default false
);

create table critical_alerts (
  id bigserial primary key,
  user_id uuid references users(id),
  level text default 'non_blocking',
  message text not null,
  is_resolved boolean default false
);
```

### نظام الصلاحيات (Row Level Security)

يستخدم النظام **Row Level Security (RLS)** من Supabase لضمان:
- 🔒 **الموظف**: يرى فقط بياناته الشخصية (نقاطه، إجاباته، إشعاراته)
- 👑 **الأدمن**: يرى جميع البيانات + يمكنه إضافة جلسات ورفع ملفات
- ✅ كل جدول لديه سياسات (Policies) خاصة تضمن الخصوصية

---

## 🖥 واجهات المستخدم (Pages & Routes)

### المسارات العامة (Public Routes)
| المسار | الصفحة | الوصف |
|--------|--------|-------|
| `/` | Landing Page | الصفحة الرئيسية التعريفية |
| `/login` | Login | تسجيل الدخول (بيانات تجريبية: emp1/1234, admin/1234) |
| `/signup` | Signup | إنشاء حساب جديد |

### المسارات المحمية — الموظف (Employee)
| المسار | الصفحة | الوصف |
|--------|--------|-------|
| `/employee` | Employee Dashboard | لوحة الموظف الرئيسية |
| `/mood` | Mood Questions | أسئلة المقياس اليومي (5 أسئلة) |
| `/points` | Points & Rewards | رصيد النقاط والمكافآت |
| `/sessions` | Sessions Calendar | تقويم الجلسات والحجز |
| `/notifications` | Notifications | الإشعارات |

### المسارات المحمية — الأدمن (Admin)
| المسار | الصفحة | الوصف |
|--------|--------|-------|
| `/admin` | Admin Dashboard | لوحة التحكم الرئيسية |
| `/upload` | Upload Files | رفع ملفات الموظفين |
| `/reports` | Reports | التقارير والتصدير |
| `/sessions` | Sessions Calendar | إدارة الجلسات + الحجز |
| `/analytics` | Analytics | تحليلات ورسوم بيانية |
| `/analytics-2` | Compare | مقارنة الأقسام |
| `/notifications` | Notifications | إدارة الإشعارات |

---

## 👥 المستخدمون والأدوار (Roles)

### 1. الموظف (Employee)
- **بيانات الدخول التجريبية**: `emp1` / `1234`
- **الصلاحيات**:
  - إكمال المقياس اليومي (مرة واحدة يومياً)
  - رصيد النقاط + المكافآت
  - حجز الجلسات المتاحة
  - رؤية إشعاراته فقط

### 2. الأدمن (Admin)
- **بيانات الدخول التجريبية**: `admin` / `1234`
- **الصلاحيات**:
  - لوحة مؤشرات الأداء (نسبة المشاركة، النقاط، حالات المتابعة)
  - تقارير مفصلة مع تصدير PDF/CSV
  - تحليلات ورسوم بيانية للأقسام
  - إضافة جلسات جديدة
  - رفع ملفات الموظفين
  - رؤية جميع البيانات

---

## 🔄 سير العمل (User Flow)

### 📌 سير عمل الموظف
```
الصفحة الرئيسية ← تسجيل الدخول (emp1/1234) ← لوحة الموظف
                                                    │
                          ┌─────────────────────────┼─────────────────────────┐
                          ▼                         ▼                         ▼
                    المقياس اليومي              النقاط والمكافآت          حجز جلسة
                          │                         │                         │
                          ▼                         ▼                         ▼
               يجيب على 5 أسئلة ← يتحقق من ← يشوف رصيده ← يشوف ← يختار جلسة
               يحصل 25 نقطة       مزاجه اليوم   النقاط +       المكافآت   ويحجز
                                                       
```

### 📌 سير عمل الأدمن
```
الصفحة الرئيسية ← تسجيل الدخول (admin/1234) ← لوحة الأدمن
                                                    │
                          ┌─────────────────────────┼─────────────────────────┐
                          ▼                         ▼                         ▼
                     لوحة المؤشرات             التقارير                 التحليلات
                          │                         │                         │
                          ▼                         ▼                         ▼
               نسبة المشاركة ← يغير الفترة ← يشوف رسوم ← إضافة جلسة
               النقاط الموزعة   (أسبوعي/شهري/سنوي)  بيانية     ← رفع ملفات
               حالات المتابعة   ويصدر PDF أو CSV               موظفين جدد
```

---

## 📸 صور توضيحية للنظام

### 1. الصفحة الرئيسية (Landing)
- **المحتوى**: شعار نبض (قلب) + عنوان + وصف + زرين (تسجيل دخول / إنشاء حساب)
- **المميزات**: بطاقات للقياس اليومي، المتابعة الداعمة، المكافآت
- **قسم الأدوار**: الموظف ↔ الأدمن

### 2. تسجيل الدخول (Login)
- **اختيار الدور**: موظف / أدمن
- **بيانات الدخول**: emp1 / 1234 أو admin / 1234
- **الدعم**: تسجيل عبر Supabase Auth أو بيانات تجريبية محلية

### 3. لوحة الموظف (Employee Dashboard)
- **حالة المعنويات**: نسبة الإنجاز اليومي + الحالة الداعمة
- **رصيد النقاط**: شريط تقدم + الرصيد الحالي + العتبة التالية
- **الجلسات القادمة**: بطاقات بحجز مباشر
- **آخر الإشعارات**: قائمة بالإشعارات الواردة

### 4. لوحة الأدمن (Admin Dashboard)
- **نظرة عامة**: نسبة المشاركة اليومية + إجمالي النقاط الموزعة
- **المتابعة**: عدد الحالات الحرجة بحاجة تواصل
- **ملاحظات الاتجاهات**: تحليل سريع

### 5. المقياس اليومي (Mood Questions)
- 5 أسئلة (الطاقة، التوتر، التركيز، الدعم، المزاج)
- كل سؤال: اختيار من 1 إلى 5
- شريط تقدم + نقاط مكتسبة (25 نقطة)
- منع التكرار (مرة واحدة يومياً)

### 6. النقاط والمكافآت (Points & Rewards)
- شريط التقدم: الرصيد الحالي مقابل العتبة التالية (500 نقطة)
- سجل المعاملات (Ledger): جميع التغييرات في النقاط
- الشارات (Badges): مكافآت عند 300, 400, 500 نقطة

### 7. تقويم الجلسات (Sessions Calendar)
- قائمة الجلسات المتاحة (العنوان، الوقت، المدرب، المقاعد المتبقية)
- زر حجز (متاح فقط للموظفين المسجلين)
- إضافة جلسة جديدة (متاح للأدمن فقط)

### 8. التقارير (Reports)
- **فلاتر**: أسبوعي / شهري / سنوي
- **ملخص**: عدد الموظفين + متوسط المزاج + حالات المتابعة
- **جدول**: الأقسام مع المتوسط والحالة
- **تصدير**: PDF + CSV

### 9. التحليلات (Analytics)
- **خط الاتجاه**: Line Chart لآخر 7 أيام
- **مقارنة الأقسام**: Bar Chart
- **التصنيف التلقائي**: بطاقات لكل موظف مع الحالة
- **حالة الأقسام**: Pie Chart

---

## 🚀 كيفية تشغيل المشروع

### المتطلبات الأساسية
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

### 1. تشغيل محلي (Local Development)
```bash
# 1. فتح المجلد
cd nabd-hr-admin

# 2. تثبيت التبعيات
npm install

# 3. تشغيل السيرفر المحلي
npm run dev

# 4. فتح المتصفح
# http://localhost:5173
```

### 2. بيانات الدخول التجريبية
| الدور | اسم المستخدم | كلمة المرور |
|-------|--------------|-------------|
| 👤 موظف | `emp1` | `1234` |
| 👑 أدمن | `admin` | `1234` |

### 3. تشغيل مع Supabase (اختياري)
```bash
# إنشاء ملف .env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# تشغيل التطبيق
npm run dev
```

### 4. بناء نسخة الإنتاج (Production Build)
```bash
npm run build
npm run preview
```

---

## 🔮 التوسعات المستقبلية (Future Work)

| المجال | التوسعة |
|--------|---------|
| **التكامل** | ربط مع Slack, Teams, Telegram لإرسال الإشعارات |
| **التحليلات المتقدمة** | AI للتنبؤ بحالات الإرهاق قبل حدوثها |
| **الاستبيانات المخصصة** | إنشاء استبيانات مخصصة من قبل الأدمن |
| **التقارير المتقدمة** | تقارير PDF احترافية مع رسومات |
| **الموظفين** | إضافة موظفين جدد عبر لوحة الأدمن |
| **الدردشة** | دعم نفسي فوري عبر الدردشة |
| **الملف الشخصي** | إدارة الملف الشخصي والصورة |
| **الإحصائيات** | إحصائيات متقدمة مع تصفية حسب القسم |
| **التطبيق الجوال** | نسخة موبايل (React Native) |
| **التقييم الدوري** | تقييم شهري شامل للمعنويات |

---

## 👩‍💻 فريق العمل

| العضو | الدور |
|-------|-------|
| **الطالبة** | Systems Development & Administration |
| - | تحليل المتطلبات |
| - | تصميم قاعدة البيانات |
| - | تطوير الواجهات الأمامية (Frontend) |
| - | إدارة المشروع والتوثيق |

**المشرف**: الدكتور — (يرجى إضافة اسم المشرف)

---

## 📁 الملحق: هيكل المجلدات الكامل

```
nabd-hr-admin/
├── .gitignore
├── CLAUDE.md
├── eslint.config.js
├── index.html
├── package-lock.json
├── package.json
├── postcss.config.js
├── README-railway.md
├── README.md
├── tailwind.config.js
├── TODO.md
├── tsconfig.json
├── tsconfig.node.json
├── vite-env.d.ts
├── vite.config.d.ts
├── vite.config.js
├── vite.config.ts
│
├── src/
│   ├── App.tsx
│   ├── index.css
│   ├── main.tsx
│   │
│   ├── auth/
│   │   ├── BootContext.tsx
│   │   └── ProtectedRoute.tsx
│   │
│   ├── components/
│   │   ├── ui/
│   │   │   ├── Badge.tsx
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── EmptyState.tsx
│   │   │   ├── index.ts
│   │   │   ├── Input.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── SkeletonLoader.tsx
│   │   │   ├── Textarea.tsx
│   │   │   └── Toast.tsx
│   │   ├── AdminUI.tsx
│   │   ├── AppShell.tsx
│   │   ├── DataState.tsx
│   │   ├── HeartLoader.tsx
│   │   └── LangToggle.tsx
│   │
│   ├── i18n/
│   │   ├── i18n.ts
│   │   └── LangContext.tsx
│   │
│   ├── lib/
│   │   ├── dashboardData.ts
│   │   └── supabaseClient.ts
│   │
│   ├── mock-data/
│   │   ├── admin.ts
│   │   ├── analytics.ts
│   │   ├── auth.ts
│   │   ├── employee.ts
│   │   ├── index.ts
│   │   ├── landing.ts
│   │   ├── mood.ts
│   │   ├── notifications.ts
│   │   ├── points.ts
│   │   ├── reports.ts
│   │   ├── sessions.ts
│   │   └── uploads.ts
│   │
│   ├── pages/
│   │   ├── AdminDashboard.tsx
│   │   ├── AnalyticsMonitoring.tsx
│   │   ├── AnalyticsMonitoring2.tsx
│   │   ├── EmployeeDashboard.tsx
│   │   ├── HeartLoaderPage.tsx
│   │   ├── Landing.tsx
│   │   ├── Login.tsx
│   │   ├── MoodQuestions.tsx
│   │   ├── NotificationSystem.tsx
│   │   ├── PointsRewards.tsx
│   │   ├── Reports.tsx
│   │   ├── SessionsCalendar.tsx
│   │   ├── Signup.tsx
│   │   └── UploadFiles.tsx
│   │
│   ├── styles/
│   │   └── fonts.css
│   │
│   └── theme/
│       └── tokens.ts
│
└── supabase/
    └── migrations/
        └── 001_init_hr_schema.sql
```

---

## ✅ ملخص المشروع

**مساحة نبض (Nabd Space)** هو مشروع متكامل يهدف إلى تحسين الصحة النفسية والرفاهية الوظيفية للموظفين في بيئات العمل. يتميز النظام بـ:

1. **عمارة نظيفة** — فصل كامل بين الطبقات (Presentation → Business Logic → Data)
2. **جاهزية فورية** — يعمل بنظام Mock Data بدون الحاجة لإعداد Backend
3. **قابلية التوسع** — جاهز للاتصال بـ Supabase عند الحاجة
4. **ثنائية اللغة** — دعم كامل للعربية (RTL) والإنجليزية (LTR)
5. **تصميم عصري** — واجهة داكنة أنيقة مع ألوان مريحة
6. **أمان متكامل** — RLS على قاعدة البيانات لحماية خصوصية الموظفين
7. **تجربة مستخدم ممتازة** — تصميم مبسط وبديهي مع تحميل أنيق (Heart Loader)

---

> 📅 **تاريخ التقديم**: (يرجى إضافة التاريخ)
> 
> 📧 **للتواصل**: (يرجى إضافة البريد الإلكتروني)
> 
> 🎓 **تحت إشراف دكتور**: (يرجى إضافة اسم الدكتور)

